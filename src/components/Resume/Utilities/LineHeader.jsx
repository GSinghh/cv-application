const LineHeader = ({ title }) => {
    return (
        <div className="education-header">
            <h2>{title}</h2>
            <div className="line"></div>
        </div>
    );
};

export default LineHeader;
